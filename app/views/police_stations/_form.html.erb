<%= form_with(model: police_station, local: true) do |form| %>
  <% if police_station.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(police_station.errors.count, "error") %> prohibited this police_station from being saved:</h2>

      <ul>
        <% police_station.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :tel %>
    <%= form.number_field :tel %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
